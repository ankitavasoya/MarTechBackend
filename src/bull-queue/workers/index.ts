import audienceBatchWorker from './audience-batch.worker';
import campaignCallWorker from './campaign-call.worker';
import campaignStepProccessWorker from './campaign-step-process.worker';

export const workers = {
  audienceBatchWorker,
  campaignCallWorker,
  campaignStepProccessWorker
};
